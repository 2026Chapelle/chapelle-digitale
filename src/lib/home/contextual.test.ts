import { describe,expect,it } from 'vitest'
import { INTENTS,resolveFirstStep,resolveLiveState,resolveHeroGreeting,selectTodayPrimary } from '@/lib/home/contextual'
describe('contextual home',()=>{it('resolves live states',()=>{const n=new Date('2026-09-05T10:00:00Z');expect(resolveLiveState([{title:'Culte',status:'live',is_live:true,youtube_url:'https://youtu.be/abcdefghijk'}],n)).toMatchObject({status:'LIVE',youtubeVideoId:'abcdefghijk'});expect(resolveLiveState([{status:'scheduled',scheduled_at:'2026-09-06T10:00:00Z'}],n).status).toBe('UPCOMING');expect(resolveLiveState([],n)).toEqual({status:'OFFLINE'})});it('keeps four intents and safe steps',()=>{expect(INTENTS).toHaveLength(4);expect(resolveFirstStep({intent:'need_prayer',authenticated:false}).href).toBe('/priere');expect(resolveFirstStep({intent:'resume_walk',authenticated:false}).href).toBe('/parcours')});it('reuses member next action',()=>{const a={kind:'formation' as const,label:'Continuer',ctaLabel:'Continuer' as const,reason:'Déjà commencé',href:'/member/dashboard/formations/x',priority:20};expect(resolveFirstStep({intent:'grow_in_faith',authenticated:true,memberNextAction:a}).cta).toBe('Continuer')})})
