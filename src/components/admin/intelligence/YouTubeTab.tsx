'use client'
/** HUB-4 — STUB (chantier 2 remplit). Onglet YouTube : Vue d'ensemble / Top
 *  contenus / Audience / Acquisition / Tendances, via /api/intelligence/youtube.
 *  États honnêtes (SOURCE=YOUTUBE, FRESHNESS, LAST_SYNC), jamais de faux chiffre. */
export default function YouTubeTab() {
  return (
    <div className="card-royal p-6 text-sm text-pearl/50">Onglet YouTube — (stub)</div>
  )
}
