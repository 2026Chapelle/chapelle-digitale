'use client'
/** HUB-4 — STUB (chantier 3 remplit). Onglet Facebook/Instagram : métriques
 *  plateforme Meta + attribution Citadelle, via /api/intelligence/meta. États
 *  honnêtes, séparation META_PLATFORM_METRICS vs CITADELLE_ATTRIBUTED_METRICS. */
export default function MetaTab() {
  return (
    <div className="card-royal p-6 text-sm text-pearl/50">Onglet Facebook / Instagram — (stub)</div>
  )
}
