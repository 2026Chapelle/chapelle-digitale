'use client'
/** HUB-4 — STUB (chantier 4 remplit). Onglet Conversions : funnel + attribution
 *  par source, à partir d'événements PROUVÉS uniquement, via
 *  /api/intelligence/conversions. Jamais de taux entre cohortes incompatibles
 *  (RATE=UNAVAILABLE + REASON). Jamais de chiffre inventé. */
export default function ConversionsTab() {
  return (
    <div className="card-royal p-6 text-sm text-pearl/50">Onglet Conversions — (stub)</div>
  )
}
