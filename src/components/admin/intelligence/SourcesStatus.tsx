'use client'
/** HUB-4 — STUB (chantier 1 remplit). Panneau « Sources de données & connexions »
 *  : état réel dynamique de chaque canal via /api/intelligence/status. */
export default function SourcesStatus() {
  return (
    <div className="card-royal p-4 text-sm text-pearl/50">
      Sources de données — chargement de l’état réel… (stub)
    </div>
  )
}
