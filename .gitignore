# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Next.js
.next/
out/
build/

# Production
dist/

# Misc
.DS_Store
*.pem
Thumbs.db

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files (NEVER commit these)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Supabase
supabase/.branches
supabase/.temp

# Artefacts de déploiement (build standalone + archive) — NE PAS versionner
deploy-citadelle/
deploy-citadelle*.zip

# Copie de projet imbriquée (projet séparé, hors source de vérité)
chapelle-digitale/

# Runtime agent
.claude/scheduled_tasks.lock

# IDE
.vscode/settings.json
.idea/
*.swp
*.swo

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# Harnais OFFLINE E2E (Lot 1.2) — env & sorties Playwright NON versionnés
# .env.offline-e2e : env FACTICE local (aucun secret) — recréé depuis la doc
.env.offline-e2e
# Sorties Playwright lourdes/temporaires (seul le rapport .md est versionné)
/artifacts/offline-playwright/html-report/
/artifacts/offline-playwright/results.json
/artifacts/offline-playwright/test-results/
